spring:
  application:
    name: blogSystem-userCenter
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      uri: http://localhost:5000/
      label: master
eureka:
  client:
    service-url:
       defaultZone: http://localhost:9000/eureka/
